apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-service-config
  namespace: nexus-mcp
  labels:
    app: nexus-mcp
    tier: data-plane
  annotations:
    nexus.ai/config-version: "v0"
data:
  TELEMETRY_MAP_STYLE: "mapbox://styles/mapbox/dark-v10"
  DISSEMINATION_ENDPOINT: "https://example.com/destination"
